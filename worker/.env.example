# Anthropic API key used by generateStoreCopy() for AI-written store listings.
# Pay-as-you-go key from https://console.anthropic.com (separate from a Claude Pro subscription).
ANTHROPIC_API_KEY=sk-ant-...

# HTTP port for the worker API (npm run server). Defaults to 4000 if unset.
PORT=4000
