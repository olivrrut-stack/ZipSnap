# Base URL of the deployed worker API (see worker/.env.example and worker/Dockerfile).
# Defaults to http://localhost:4000 for local development.
NEXT_PUBLIC_WORKER_URL=https://your-worker.up.railway.app
